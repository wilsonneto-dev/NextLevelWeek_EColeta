class Item {
  id: number;
  image: string;
  title: string;

  constructor(id: number, image: string, title: string) {
    this.id = id;
    this.image = image;
    this.title = title;
  }
}

export default Item;
